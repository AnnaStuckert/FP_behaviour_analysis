facet_wrap(~ genotype, ncol = 2)
View(df)
# Loading
library(pacman)
p_load(readxl, tidyverse)
p_load(broom, ggsignif, multcomp, emmeans)
# xlsx files
#df <- read_excel("3CT.xlsx") #using the trial for 372 preinduction that was redone due to folding cable
df <- read_excel("3CT.xlsx") #using the firt, incomplete trial for 372 that had folding cable
# Group assignment for each mouse_id
group_assignments <- c(
"423" = "TDP-43",
"372" = "TDP-43",
"376" = "TDP-43",
"374" = "WT",
"429" = "WT"
)
class(df$ID)
# Add group column to mean_per_mouse
df <- df %>%
mutate(genotype = group_assignments[as.character(ID)])
df <- df %>% mutate(social_time=(`In zone SocialZone multiCond cumulative s`+df$SocialZoneRound))
df <- df %>% mutate(novel_time=(`In zone NoveltyZone multiCond cumulative s`+df$NoveltyZoneRound))
#use this for LMM approach with full sample size
habituation_cond <-  df %>% filter(Condition == "Hab")
social_cond<-  df %>% filter(Condition == "Social")
novelty_cond<-  df %>% filter(Condition == "Novel")
habituation_cond <- habituation_cond %>% mutate(social_preference_relative=((`In zone SocialZone multiCond cumulative s`-`In zone NoveltyZone multiCond cumulative s`)/(`In zone NoveltyZone multiCond cumulative s`+`In zone SocialZone multiCond cumulative s`)))
social_cond <- social_cond %>% mutate(social_preference_relative=((social_cond$social_time-social_cond$novel_time)/(social_cond$social_time+social_cond$novel_time)))
novelty_cond <- novelty_cond %>% mutate(novelty_preference_relative=((novelty_cond$novel_time-novelty_cond$social_time)/(novelty_cond$novel_time+novelty_cond$social_time)))
#Loading packages for linear mixed effects modelling
library(lmerTest)
library(lme4)
social_cond$Week <- factor(social_cond$Week, levels = c("Preinduction","1st week post induction","2nd week post induction"))
#creating a linear model of the same variables as included in the anova
m_anova <-lmer(social_preference_relative ~  Week + (1|ID), data=social_cond)
summary(m_anova)
# Obtain estimated marginal means
emmeans_results <- emmeans(m_anova,pairwise ~ Week, conf.level = 0.95)
emmeans_results
class(emmeans_results)
emm_single <- emmeans_results[[1]]  # Extract the first emmGrid object
class(emm_single)
pairwise_results <- contrast(emm_single, method = "pairwise")
class(pairwise_results)
#pairwise_results <- lapply(emmeans_results, contrast, method = "pairwise")
# Perform pairwise comparisons
#pairwise_results <- contrast(emmeans_results, method = "pairwise")
# Display the results
summary(pairwise_results)
#bonferroni
#pwc <- pairs(emmeans_results, adjust = "bon")
#pwc
pwc <- contrast(emm_single, method = "pairwise", adjust = "bon")
summary(pwc)
#Loading packages for linear mixed effects modelling
library(lmerTest)
library(lme4)
social_cond$Week <- factor(social_cond$Week, levels = c("Preinduction","1st week post induction","2nd week post induction"))
#creating a linear model of the same variables as included in the anova
m_anova <-lmer(social_preference_relative ~  Week + genotype + (1|ID), data=social_cond)
summary(m_anova)
# Obtain estimated marginal means
emmeans_results <- emmeans(m_anova,pairwise ~ Week, conf.level = 0.95)
emmeans_results
class(emmeans_results)
emm_single <- emmeans_results[[1]]  # Extract the first emmGrid object
class(emm_single)
pairwise_results <- contrast(emm_single, method = "pairwise")
class(pairwise_results)
#pairwise_results <- lapply(emmeans_results, contrast, method = "pairwise")
# Perform pairwise comparisons
#pairwise_results <- contrast(emmeans_results, method = "pairwise")
# Display the results
summary(pairwise_results)
#bonferroni
#pwc <- pairs(emmeans_results, adjust = "bon")
#pwc
pwc <- contrast(emm_single, method = "pairwise", adjust = "bon")
summary(pwc)
social_cond_TDP <-  social_cond %>% filter(genotype == "TDP-43")
View(social_cond_TDP)
#Loading packages for linear mixed effects modelling
library(lmerTest)
library(lme4)
social_cond_TDP$Week <- factor(social_cond_TDP$Week, levels = c("Preinduction","1st week post induction","2nd week post induction"))
#creating a linear model of the same variables as included in the anova
m_anova <-lmer(social_preference_relative ~  Week + (1|ID), data=social_cond_TDP)
summary(m_anova)
# Obtain estimated marginal means
emmeans_results <- emmeans(m_anova,pairwise ~ Week, conf.level = 0.95)
emmeans_results
class(emmeans_results)
emm_single <- emmeans_results[[1]]  # Extract the first emmGrid object
class(emm_single)
pairwise_results <- contrast(emm_single, method = "pairwise")
class(pairwise_results)
#pairwise_results <- lapply(emmeans_results, contrast, method = "pairwise")
# Perform pairwise comparisons
#pairwise_results <- contrast(emmeans_results, method = "pairwise")
# Display the results
summary(pairwise_results)
#bonferroni
#pwc <- pairs(emmeans_results, adjust = "bon")
#pwc
pwc <- contrast(emm_single, method = "pairwise", adjust = "bon")
summary(pwc)
social_cond$Week <- factor(social_cond$Week , levels = c("Preinduction","1st week post induction"))
p_load(ggbeeswarm, ggsignif)
library(ggplot2)
library(ggbeeswarm)
library(ggsignif)
library(dplyr)
ggplot(social_cond, aes(x = Week, y = social_preference_relative)) +
geom_beeswarm(size = 2.5, aes(color = ID)) +
ylab("Social Preference") +
xlab("Timepoint") +
# geom_signif(
#   comparisons = list(c("Preinduction", "1st week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.3,
#   annotations = c("0.1283")
# ) +
# geom_signif(
#   comparisons = list(c("Preinduction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.15,
#   annotations = c("0.0087")
# ) +
# geom_signif(
#   comparisons = list(c("1st week post induction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1,
#   annotations = c("0.4487")
# ) +
geom_crossbar(stat = "summary", fun = mean, width = 0.2, fatten = 1.5) +
geom_errorbar(stat = "summary", fun.data = mean_se, width = 0.1, position = "dodge") +
theme(text = element_text(size = 13)) +
facet_wrap(~ genotype, ncol = 2)
# Loading
library(pacman)
p_load(readxl, tidyverse)
p_load(broom, ggsignif, multcomp, emmeans)
# xlsx files
#df <- read_excel("3CT.xlsx") #using the trial for 372 preinduction that was redone due to folding cable
df <- read_excel("3CT_372_pre_FirstTrialUsed.xlsx") #using the firt, incomplete trial for 372 that had folding cable
# Group assignment for each mouse_id
group_assignments <- c(
"423" = "TDP-43",
"372" = "TDP-43",
"376" = "TDP-43",
"374" = "WT",
"429" = "WT"
)
class(df$ID)
# Add group column to mean_per_mouse
df <- df %>%
mutate(genotype = group_assignments[as.character(ID)])
df <- df %>% mutate(social_time=(`In zone SocialZone multiCond cumulative s`+df$SocialZoneRound))
df <- df %>% mutate(novel_time=(`In zone NoveltyZone multiCond cumulative s`+df$NoveltyZoneRound))
#use this for LMM approach with full sample size
habituation_cond <-  df %>% filter(Condition == "Hab")
social_cond<-  df %>% filter(Condition == "Social")
novelty_cond<-  df %>% filter(Condition == "Novel")
habituation_cond <- habituation_cond %>% mutate(social_preference_relative=((`In zone SocialZone multiCond cumulative s`-`In zone NoveltyZone multiCond cumulative s`)/(`In zone NoveltyZone multiCond cumulative s`+`In zone SocialZone multiCond cumulative s`)))
social_cond <- social_cond %>% mutate(social_preference_relative=((social_cond$social_time-social_cond$novel_time)/(social_cond$social_time+social_cond$novel_time)))
novelty_cond <- novelty_cond %>% mutate(novelty_preference_relative=((novelty_cond$novel_time-novelty_cond$social_time)/(novelty_cond$novel_time+novelty_cond$social_time)))
social_cond_TDP <-  social_cond %>% filter(genotype == "TDP-43")
#Loading packages for linear mixed effects modelling
library(lmerTest)
library(lme4)
social_cond_TDP$Week <- factor(social_cond_TDP$Week, levels = c("Preinduction","1st week post induction","2nd week post induction"))
#creating a linear model of the same variables as included in the anova
m_anova <-lmer(social_preference_relative ~  Week + (1|ID), data=social_cond_TDP)
summary(m_anova)
# Obtain estimated marginal means
emmeans_results <- emmeans(m_anova,pairwise ~ Week, conf.level = 0.95)
emmeans_results
class(emmeans_results)
emm_single <- emmeans_results[[1]]  # Extract the first emmGrid object
class(emm_single)
pairwise_results <- contrast(emm_single, method = "pairwise")
class(pairwise_results)
#pairwise_results <- lapply(emmeans_results, contrast, method = "pairwise")
# Perform pairwise comparisons
#pairwise_results <- contrast(emmeans_results, method = "pairwise")
# Display the results
summary(pairwise_results)
#bonferroni
#pwc <- pairs(emmeans_results, adjust = "bon")
#pwc
pwc <- contrast(emm_single, method = "pairwise", adjust = "bon")
summary(pwc)
social_cond$Week <- factor(social_cond$Week , levels = c("Preinduction","1st week post induction"))
p_load(ggbeeswarm, ggsignif)
library(ggplot2)
library(ggbeeswarm)
library(ggsignif)
library(dplyr)
ggplot(social_cond, aes(x = Week, y = social_preference_relative)) +
geom_beeswarm(size = 2.5, aes(color = ID)) +
ylab("Social Preference") +
xlab("Timepoint") +
# geom_signif(
#   comparisons = list(c("Preinduction", "1st week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.3,
#   annotations = c("0.1283")
# ) +
# geom_signif(
#   comparisons = list(c("Preinduction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.15,
#   annotations = c("0.0087")
# ) +
# geom_signif(
#   comparisons = list(c("1st week post induction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1,
#   annotations = c("0.4487")
# ) +
geom_crossbar(stat = "summary", fun = mean, width = 0.2, fatten = 1.5) +
geom_errorbar(stat = "summary", fun.data = mean_se, width = 0.1, position = "dodge") +
theme(text = element_text(size = 13)) +
facet_wrap(~ genotype, ncol = 2)
#Loading packages for linear mixed effects modelling
library(lmerTest)
library(lme4)
novelty_cond$Week <- factor(novelty_cond$Week , levels = c("Preinduction","1st week post induction","2nd week post induction"))
#creating a linear model of the same variables as included in the anova
m_anova <-lmer(novelty_preference_relative ~  Week + (1|ID), data=novelty_cond)
summary(m_anova)
# Obtain estimated marginal means
emmeans_results <- emmeans(m_anova, ~ Week)
# Perform pairwise comparisons
pairwise_results <- contrast(emmeans_results, method = "pairwise")
# Display the results
summary(pairwise_results)
#bonferroni
pwc <- pairs(emmeans_results, adjust = "bon")
pwc
novelty_cond_TDP <-  novelty_cond %>% filter(genotype == "TDP-43")
#Loading packages for linear mixed effects modelling
library(lmerTest)
library(lme4)
novelty_cond_TDP$Week <- factor(novelty_cond_TDP$Week , levels = c("Preinduction","1st week post induction","2nd week post induction"))
#creating a linear model of the same variables as included in the anova
m_anova <-lmer(novelty_preference_relative ~  Week + (1|ID), data=novelty_cond_TDP)
summary(m_anova)
# Obtain estimated marginal means
emmeans_results <- emmeans(m_anova, ~ Week)
# Perform pairwise comparisons
pairwise_results <- contrast(emmeans_results, method = "pairwise")
# Display the results
summary(pairwise_results)
#bonferroni
pwc <- pairs(emmeans_results, adjust = "bon")
pwc
novelty_cond$Week <- factor(novelty_cond$Week , levels = c("Preinduction","1st week post induction"))
ggplot(novelty_cond, aes(x = Week, y = novelty_preference_relative)) +
geom_beeswarm(size = 2.5, aes(color = ID)) +
ylab("Novelty Preference") +
xlab("Timepoint") +
# geom_signif(
#   comparisons = list(c("Preinduction", "1st week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.3,
#   annotations = c("0.1283")
# ) +
# geom_signif(
#   comparisons = list(c("Preinduction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.15,
#   annotations = c("0.0087")
# ) +
# geom_signif(
#   comparisons = list(c("1st week post induction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1,
#   annotations = c("0.4487")
# ) +
geom_crossbar(stat = "summary", fun = mean, width = 0.2, fatten = 1.5) +
geom_errorbar(stat = "summary", fun.data = mean_se, width = 0.1, position = "dodge") +
theme(text = element_text(size = 13)) +
facet_wrap(~ genotype, ncol = 2)
knitr::opts_chunk$set(echo = TRUE)
# Loading
library(pacman)
p_load(readxl, tidyverse)
# xlsx files
df <- read_excel("EPM.xlsx")
# Group assignment for each mouse_id
group_assignments <- c(
"423" = "TDP-43",
"372" = "TDP-43",
"376" = "TDP-43",
"374" = "WT",
"429" = "WT"
)
class(df$ID)
# Add group column to mean_per_mouse
df <- df %>%
mutate(genotype = group_assignments[as.character(ID)])
#periphery_preference = time spent in periphery / time spent in center
df<-  df %>% mutate(time_dark=df$`In zone Closed_arm1 cumulative`+df$`In zone Closed_arm2 cumulative`)
df<-  df %>% mutate(time_light=df$`In zone Open_arm1 cumulative`+df$`In zone Open_arm2 cumaltive`)
#
# #relative time spent in light over dark
df<-  df %>% mutate(light_preference=time_light/(time_light+time_dark)*100)
#relative time spent in dark over light
df<-  df %>% mutate(dark_preference=time_dark/(time_light+time_dark)*100)
#frequency in dark and light arms
df<-  df %>% mutate(freq_dark=df$`In zone Closed_arm1  frequency`+df$`In zone Closed_arm2 frequency`)
df<-  df %>% mutate(freq_light=df$`In zone Open_arm1 frequency`+df$`In zone Open_arm2 frequency`)
#relative time spent in dark over light as entries in open arm / total arm entries
df<-  df %>% mutate(light_preference_freq=freq_light/(freq_light+freq_dark)*100)
df<-  df %>% mutate(dark_preference_freq=freq_dark/(freq_light+freq_dark)*100)
#periphery_preference = time spent in periphery / time spent in center
df<-  df %>% mutate(time_dark=df$`In zone Closed_arm1 cumulative`+df$`In zone Closed_arm2 cumulative`)
df<-  df %>% mutate(time_light=df$`In zone Open_arm1 cumulative`+df$`In zone Open_arm2 cumaltive`)
#
# #relative time spent in light over dark
df<-  df %>% mutate(light_preference=time_light/(time_light+time_dark)*100)
#relative time spent in dark over light
df<-  df %>% mutate(dark_preference=time_dark/(time_light+time_dark)*100)
#frequency in dark and light arms
df<-  df %>% mutate(freq_dark=df$`In zone Closed_arm1  frequency`+df$`In zone Closed_arm2 frequency`)
df<-  df %>% mutate(freq_light=df$`In zone Open_arm1 frequency`+df$`In zone Open_arm2 frequency`)
#relative time spent in dark over light as entries in open arm / total arm entries
df<-  df %>% mutate(light_preference_freq=freq_light/(freq_light+freq_dark)*100)
df<-  df %>% mutate(dark_preference_freq=freq_dark/(freq_light+freq_dark)*100)
#Loading packages for linear mixed effects modelling
library(lmerTest)
library(lme4)
#creating a linear model of the same variables as included in the anova
m_anova <-lmer(dark_preference ~  Week + (1|ID), data=df_TDP)
df_TDP <-  social_cond %>% filter(genotype == "TDP-43")
df_TDP <-  df %>% filter(genotype == "TDP-43")
#Loading packages for linear mixed effects modelling
library(lmerTest)
library(lme4)
#creating a linear model of the same variables as included in the anova
m_anova <-lmer(dark_preference ~  Week + (1|ID), data=df_TDP)
summary(m_anova)
# Obtain estimated marginal means
emmeans_results <- emmeans(m_anova, ~ Week)
# Perform pairwise comparisons
pairwise_results <- contrast(emmeans_results, method = "pairwise")
# Display the results
summary(pairwise_results)
#bonferroni
pwc <- pairs(emmeans_results, adjust = "bon")
pwc
p_load(ggbeeswarm, ggsignif)
ggplot(df, aes(x = Week, y = dark_preference)) +
geom_beeswarm(size = 2.5, aes(color = ID)) +
ylab("Social Preference") +
xlab("Timepoint") +
# geom_signif(
#   comparisons = list(c("Preinduction", "1st week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.3,
#   annotations = c("0.1283")
# ) +
# geom_signif(
#   comparisons = list(c("Preinduction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.15,
#   annotations = c("0.0087")
# ) +
# geom_signif(
#   comparisons = list(c("1st week post induction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1,
#   annotations = c("0.4487")
# ) +
geom_crossbar(stat = "summary", fun = mean, width = 0.2, fatten = 1.5) +
geom_errorbar(stat = "summary", fun.data = mean_se, width = 0.1, position = "dodge") +
theme(text = element_text(size = 13)) +
facet_wrap(~ genotype, ncol = 2)
p_load(ggbeeswarm, ggsignif)
df$Week <- factor(df$Week , levels = c("Preinduction","1st week post induction"))
ggplot(df, aes(x = Week, y = dark_preference)) +
geom_beeswarm(size = 2.5, aes(color = ID)) +
ylab("Social Preference") +
xlab("Timepoint") +
# geom_signif(
#   comparisons = list(c("Preinduction", "1st week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.3,
#   annotations = c("0.1283")
# ) +
# geom_signif(
#   comparisons = list(c("Preinduction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.15,
#   annotations = c("0.0087")
# ) +
# geom_signif(
#   comparisons = list(c("1st week post induction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1,
#   annotations = c("0.4487")
# ) +
geom_crossbar(stat = "summary", fun = mean, width = 0.2, fatten = 1.5) +
geom_errorbar(stat = "summary", fun.data = mean_se, width = 0.1, position = "dodge") +
theme(text = element_text(size = 13)) +
facet_wrap(~ genotype, ncol = 2)
knitr::opts_chunk$set(echo = TRUE)
# Loading
library(pacman)
p_load(readxl, tidyverse)
# xlsx files
df <- read_excel("EPM.xlsx")
# Group assignment for each mouse_id
group_assignments <- c(
"423" = "TDP-43",
"372" = "TDP-43",
"376" = "TDP-43",
"374" = "WT",
"429" = "WT"
)
class(df$ID)
# Add group column to mean_per_mouse
df <- df %>%
mutate(genotype = group_assignments[as.character(ID)])
#periphery_preference = time spent in periphery / time spent in center
df<-  df %>% mutate(time_dark=df$`In zone Closed_arm1 cumulative`+df$`In zone Closed_arm2 cumulative`)
df<-  df %>% mutate(time_light=df$`In zone Open_arm1 cumulative`+df$`In zone Open_arm2 cumaltive`)
#
# #relative time spent in light over dark
df<-  df %>% mutate(light_preference=time_light/(time_light+time_dark)*100)
#relative time spent in dark over light
df<-  df %>% mutate(dark_preference=time_dark/(time_light+time_dark)*100)
#frequency in dark and light arms
df<-  df %>% mutate(freq_dark=df$`In zone Closed_arm1  frequency`+df$`In zone Closed_arm2 frequency`)
df<-  df %>% mutate(freq_light=df$`In zone Open_arm1 frequency`+df$`In zone Open_arm2 frequency`)
#relative time spent in dark over light as entries in open arm / total arm entries
df<-  df %>% mutate(light_preference_freq=freq_light/(freq_light+freq_dark)*100)
df<-  df %>% mutate(dark_preference_freq=freq_dark/(freq_light+freq_dark)*100)
df_TDP <-  df %>% filter(genotype == "TDP-43")
#Loading packages for linear mixed effects modelling
library(lmerTest)
library(lme4)
#creating a linear model of the same variables as included in the anova
m_anova <-lmer(dark_preference ~  Week + (1|ID), data=df_TDP)
summary(m_anova)
# Obtain estimated marginal means
emmeans_results <- emmeans(m_anova, ~ Week)
# Perform pairwise comparisons
pairwise_results <- contrast(emmeans_results, method = "pairwise")
# Display the results
summary(pairwise_results)
#bonferroni
pwc <- pairs(emmeans_results, adjust = "bon")
pwc
p_load(ggbeeswarm, ggsignif)
df$Week <- factor(df$Week , levels = c("Preinduction","1st week post induction"))
ggplot(df, aes(x = Week, y = dark_preference)) +
geom_beeswarm(size = 2.5, aes(color = ID)) +
ylab("Social Preference") +
xlab("Timepoint") +
# geom_signif(
#   comparisons = list(c("Preinduction", "1st week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.3,
#   annotations = c("0.1283")
# ) +
# geom_signif(
#   comparisons = list(c("Preinduction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.15,
#   annotations = c("0.0087")
# ) +
# geom_signif(
#   comparisons = list(c("1st week post induction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1,
#   annotations = c("0.4487")
# ) +
geom_crossbar(stat = "summary", fun = mean, width = 0.2, fatten = 1.5) +
geom_errorbar(stat = "summary", fun.data = mean_se, width = 0.1, position = "dodge") +
theme(text = element_text(size = 13)) +
facet_wrap(~ genotype, ncol = 2)
p_load(ggbeeswarm, ggsignif)
df$Week <- factor(df$Week , levels = c("Preinduction","1st week post induction"))
ggplot(df, aes(x = Week, y = dark_preference)) +
geom_beeswarm(size = 2.5, aes(color = ID)) +
ylab("Darkness Preference") +
xlab("Timepoint") +
# geom_signif(
#   comparisons = list(c("Preinduction", "1st week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.3,
#   annotations = c("0.1283")
# ) +
# geom_signif(
#   comparisons = list(c("Preinduction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1.15,
#   annotations = c("0.0087")
# ) +
# geom_signif(
#   comparisons = list(c("1st week post induction", "2nd week post induction")),
#   map_signif_level = TRUE,
#   y_position = 1,
#   annotations = c("0.4487")
# ) +
geom_crossbar(stat = "summary", fun = mean, width = 0.2, fatten = 1.5) +
geom_errorbar(stat = "summary", fun.data = mean_se, width = 0.1, position = "dodge") +
theme(text = element_text(size = 13)) +
facet_wrap(~ genotype, ncol = 2)
